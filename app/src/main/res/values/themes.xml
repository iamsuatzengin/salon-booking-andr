<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SalonBooking" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SalonBooking" parent="Base.Theme.SalonBooking" />

    <style name="OtpEditTextItem" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/bg_otp_edit_text</item>
        <item name="android:cursorVisible">false</item>
        <item name="android:inputType">number</item>
        <item name="android:digits">0123456789</item>
        <item name="android:longClickable">false</item>
        <item name="android:maxLength">1</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:enabled">false</item>
        <item name="android:textColor">@color/black</item>
    </style>
</resources>